import { ReplaceAndTrimPipe } from './replace-and-trim.pipe';

describe('ReplaceAndTrimPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceAndTrimPipe();
    expect(pipe).toBeTruthy();
  });
});
